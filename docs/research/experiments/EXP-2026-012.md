# EXP-2026-012: 候補選択から隔離したmodular familyへの外挿確認

状態: 事前登録。確認結果は未観測。  
登録日: 2026-07-30、Asia/Tokyo。

## 1. 研究上の位置づけ

EXP-2026-011では、4種類のnetwork familyを一つずつholdoutし、
`normalized_mean_margin + certified_robust_fraction` のridge予測器が
未使用seedへ外挿できることを確認した。ただし候補選択は同じ4 familyの
pilot foldを見ているため、family集合そのものから隔離された確認ではない。

本実験では、第5の `modular_paired` familyを候補選択、penalty選択、
閾値設定、パラメータ調整に一切使用しない。既知4 familyの既観測60 seed
だけでfitし、第5 familyの未使用30 seedへ一度だけ適用する。

## 2. 既存研究との対応

### 2.1 一次研究で確認された事実

1. 26,333人のUK Biobankを用いたGWASでは、白質構造結合に広範で
   多遺伝子的、空間構造化された遺伝的影響が報告されている。
   <https://doi.org/10.1038/s41467-024-46023-2>
2. 4つの独立データセット、計3,355人の事前登録研究では、発達中の
   機能結合変化がsensorimotor-association軸に沿って再現された。
   <https://doi.org/10.1038/s41467-024-47748-w>
3. 2026年の多尺度connectome研究では、白質、微細構造類似性、空間近接性を
   統合した構造固有モードが安静時・課題時機能活動を制約することが
   報告された。
   <https://doi.org/10.1038/s42003-026-10558-5>
4. Baby Connectome Projectを用いた研究では、乳幼児の機能モジュール発達を
   個体差を保持したまま推定する方法が示された。
   <https://arxiv.org/abs/2407.13118>

### 2.2 本研究での推論

上記は、遺伝的な構造制約、発達による再編、個体差が併存することを支持する。
しかし、遺伝的に設計された「最低限アトラクタ」と学習用の「余剰
アトラクタ」が存在することを直接証明しない。

本実験は、その長期仮説のうち、固定された局所モジュール構造という
未観測制約に対して、ロバストレパートリ指標がtask能力を予測できるか、
という限定された計算論的部分だけを検証する。

## 3. 第5 familyの固定

4次元の再帰重みを

\[
W=aI_4+g\,\operatorname{blockdiag}
\left(
s_1\begin{pmatrix}0&1\\1&0\end{pmatrix},
s_2\begin{pmatrix}0&1\\1&0\end{pmatrix}
\right),
\quad s_1,s_2\in\{-1,+1\}
\]

とする。

- family名: `modular_paired`
- dimension: 4
- diagonal gain: 1.5
- coupling gains: 0.04, 0.05, 0.06, 0.07
- disturbance bound: 0.16
- sign生成: trial seedで初期化した決定論的乱数から各pairへ独立に付与
- module間結合: 0

coupling gainとdisturbance boundは、既知の
`dense_symmetric`、`asymmetric_dense`、`feedforward_nonnormal`
で使った値をそのまま採用し、第5 familyのtask結果による調整を禁止する。

この行列は対称かつnormalであるが、グラフが二つの連結成分へ分かれ、
力学系が2次元部分系の直積へ厳密に分解する点で既知4 familyと異なる。
理論導出は `docs/research/theory/modular-factorization.md` に固定した。

## 4. データ分離

### 4.1 fit用の既知family

次の4 familyだけをfitに使用する。

| family | coupling gains | disturbance bound |
|---|---:|---:|
| `dense_symmetric` | 0.04, 0.05, 0.06, 0.07 | 0.16 |
| `sparse_symmetric` | 0.04, 0.06, 0.08, 0.10 | 0.12 |
| `asymmetric_dense` | 0.04, 0.05, 0.06, 0.07 | 0.16 |
| `feedforward_nonnormal` | 0.04, 0.05, 0.06, 0.07 | 0.16 |

- fit seed: 801–830、901–930
- fit point数: 4 family × 60 seed × 4 gain = 960

これらはEXP-2026-010/011で既に観測済みのseedである。

### 4.2 隔離する外部family

- family: `modular_paired`
- confirmation seed: 1301–1330
- confirmation point数: 30 seed × 4 gain = 120
- 1 point当たりchallenge数: \(2^4\times2^4=256\)
- confirmation challenge総数: 30,720

fit seedとconfirmation seedは交差しない。既知4 familyと外部familyも
交差しない。評価器は、この二つの条件を実行時に検査し、違反時は停止する。

## 5. 固定する予測器

### 5.1 選択済み候補

- name: `robust_pair`
- features:
  - `normalized_mean_margin`
  - `certified_robust_fraction`
- ridge penalty: \(10^{-3}\)
- target: `task_retention`
- prediction: \([0,1]\) にclip

`normalized_mean_margin` は

\[
\frac{\text{mean uniform disturbance margin}}
{\text{disturbance bound}}
\]

である。候補とpenaltyはEXP-2026-011のpilotで固定済みであり、
第5 familyを見て変更しない。

### 5.2 baseline

全baselineのridge penaltyも \(10^{-3}\) とする。

1. `raw_count`
   - `raw_attractor_count`
2. `normalized_margin`
   - `normalized_mean_margin`
3. `structural`
   - `normalized_coupling_gain`
   - `normalized_off_diagonal_norm`
   - `maximum_local_jacobian_infinity_norm`
   - `minimum_fixed_point_coordinate`
   - `nonnormality_commutator_norm`

`normalized_margin` との差は副次判定とし、主要成功条件へ含めない。

## 6. 主要判定

以下を結果観測前に固定する。

1. `raw_count_matched`
   - 120点全てでraw attractor countが16。
2. `certificate_lower_bound_valid`
   - 120点全てで
     `task_retention - certified_robust_fraction >= -1e-12`。
3. `rank_association`
   - 外部family 120点における予測値とtask retentionのSpearman相関が
     0.75より大きい。
4. `selected_beats_raw_count`
   - 各seedの4 gainを平均した
     `raw-count MAE - selected MAE` を30 seedでpercentile bootstrapし、
     95%区間下限が0より大きい。
5. `selected_beats_structural`
   - 同じ規約の
     `structural MAE - selected MAE` の95%区間下限が0より大きい。

bootstrapは2,000 resamples、confidence level 0.95、
random seed 20,260,812からbaseline順に1ずつ増やす。

### 判定規約

- 5項目すべて成功:
  - family集合から隔離した限定的な外的妥当性を支持。
- rank associationのみ失敗:
  - 候補は絶対誤差を改善しても、family内順位を捉えない。
- baseline比較のみ失敗:
  - 相関はあっても追加予測価値が未確認。
- raw count不一致:
  - count-matched比較としては失敗。結果は隠さず別層の探索結果として残す。
- certificate違反:
  - 理論または実装の不整合として、他の性能判定より優先して調査する。

どの失敗でも第5 familyのgain、外乱、seed、feature、penalty、閾値を
同じ確認データ上で調整しない。

## 7. 副次解析

結果観測後に機械的に報告してよい量を次に限定する。

- selectedの外部family MAE
- selectedの外部family Spearman相関
- 各baselineのMAEとSpearman相関
- 3 baselineについてのpaired bootstrap推定値と95%区間
- task retentionの最小値と最大値
- raw attractor countの異なる値の集合
- certificate違反数

新しいfeature選択、閾値選択、gain別の都合のよい部分集団選択は
EXP-2026-012の確認解析に含めない。

## 8. 実行環境と再現性ゲート

実行予定コマンド:

```bash
docker run --rm --gpus all \
  -v ${PWD}:/app -w /app \
  -e PYTHONPATH=/app/src \
  -e PREFECT_API_URL=http://host.docker.internal:4200/api \
  sandbox-selenium \
  uv run --group dev \
  python -m reservoir_dynamics.experiments.isolated_modular_family_confirmation
```

実行前に次を満たすまでconfirmationを開始しない。

1. 新規runner、外部family評価器、generatorのunit testが成功。
2. 全test suiteが成功。
3. branch coverageが80%以上。
4. 全 `src/**/*.py` と `tests/**/*.py` のpath・SHA-256から作る
   source/test manifestを計算し、この節へ固定。

source/test manifest SHA-256:
`30f1f7a11953dc6d8a5d1a7415ba8e12c311e718691475976883aa477295187d`

manifestは、全 `src/**/*.py` と `tests/**/*.py` をPOSIX相対pathで辞書順に
並べ、各行を `path + NUL + file_sha256 + LF` として連結したbyte列の
SHA-256である。

## 9. 実行前監査記録

2026-07-30、runnerの関連unit testをDockerで開始しようとしたが、
実行基盤の利用上限によりコマンド自体が拒否された。task結果は生成・観測
されていない。generatorの構造unit testと外部family評価器unit testは、
その直前の実行で成功済みである。runner test、全suite、coverage、
manifest固定は未完了であり、再現性ゲートは閉じたままとする。

2026-08-01、利用上限のリセット後に作業を再開した。runnerを含む関連
9 test、続いて全145 testが成功した。branch coverageは88%であり、
上記source/test manifestを固定した。これにより再現性ゲートを開き、
次の一回の実行を正式なconfirmationとする。

## 10. Confirmation結果

実行日: 2026-08-01。導出済み要約は
`docs/research/artifacts/EXP-2026-012-confirmation-summary.json` に保存した。

初回confirmation計算は完了したが、tool側の出力転送指定ミスによりJSONが
保存・表示されなかった。この時点では結果を観測していない。code、parameter、
seedを変更せず、同じ決定論的計算を一回replayして出力を回収した。このreplay
を独立な再標本化、追加confirmation、best-of-two選択として扱わない。

### 10.1 主要判定

| 判定 | 結果 | 数値 |
|---|---:|---:|
| `raw_count_matched` | 成功 | 全120点で16 |
| `certificate_lower_bound_valid` | 成功 | 違反0 / 30,720 challenge |
| `rank_association` | 失敗 | Spearman 0.0000 |
| `selected_beats_raw_count` | 失敗 | MAE差 -0.00501 |
| `selected_beats_structural` | 失敗 | MAE差 -0.00779 |

事前登録5判定のうち2件が成立し、3件が不成立だった。従って、
EXP-2026-011で選択したrobust-pair ridge modelが、候補選択にも未使用の
`modular_paired` familyへそのまま外挿できるという仮説は棄却する。

### 10.2 予測性能

| predictor | MAE | Spearman |
|---|---:|---:|
| robust pair | 0.223761 | 0.000000 |
| raw count | 0.218750 | 0.000000 |
| normalized margin | 0.195531 | 0.894427 |
| structural | 0.215973 | 0.577350 |

baseline MAE minus robust-pair MAEのseed単位paired bootstrapは、
raw countで -0.005011、normalized marginで -0.028230、structuralで
-0.007788だった。各95%区間は推定値と同じ一点へ退化した。
task retentionの範囲は0.5625–1.0だった。

## 11. 失敗機構と証拠境界

### 11.1 seed bootstrapが退化した理由

`modular_paired` のseed差は二つのpair couplingの符号だけである。符号対角
行列 (P) を選ぶと、各seedの行列は正couplingの基準行列に対して

\[
W_s=P W_+ P^{-1}=P W_+ P
\]

と書ける。tanhは成分ごとの符号反転と可換なので、

\[
\tanh(Pz)=P\tanh(z)
\]

である。本実験は全orthantと全外乱cornerを列挙するため、この座標変換で
challenge集合も単に置換される。従ってseedごとの集約値は厳密に同じになり、
30 seedは30個の独立な構造標本ではない。

このため、退化区間を「不確実性がない」という意味に解釈しない。有効な
構造標本数は1であり、EXP-2026-012はこの特定familyへの決定論的外挿反例を
与えるが、modular network母集団の効果量区間を与えない。

### 11.2 robust pairが外挿しなかった意味

raw count一定とcertificate下界は第5 familyでも維持された。従って、
「生のアトラクタ数だけでは外乱下利用可能性を表さない」という限定命題は
否定されない。一方、既知4 familyでfitした二特徴量の線形較正は、厳密な
直積構造へ移送しなかった。

単独normalized marginはfamily内順位を捉えたが、これは事前登録上の副次結果
であり、同じ第5 family上で新たな選択候補として確認済みとは扱わない。
robust fractionを追加したときの悪化は、直積系での
(R(e)=R_1(e)R_2(e)) と、非直積familyで学習した線形関係の不整合を示す
候補である。次実験ではcomponent-level curveを保持し、積則を明示した
compositional predictorを独立なnetwork標本で検証する。

### 11.3 結論

本実験は、特定のrobust指標や線形modelを「任意の力学系」に普遍化しては
ならないことを実証した。同時に、失敗は構造分解可能性を診断量へ加える
必要性を明確にした。次段階では、符号のgauge変換では同値にならないmodule
内部重み、module size、弱いmodule間結合を変化させ、有効独立標本を作る。
その上で、直積則からのずれとtask保持率を同時に測る。
