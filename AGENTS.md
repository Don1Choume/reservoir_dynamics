# エンジニアリング規則

## プロジェクト概要

ここではリザバー計算におけるダイナミクスの分析と調整を可能にする研究を行っている。
非線形力学に関する知見やネットワーク分析に関する知見など、さまざまな分野の最新知見を統合し、リザバー計算の性能を最大化するとともに、高度なレベルでの調整を可能にする。
最終的には、任意のリザバーに対し分析と調整を可能とするツールを開発することを目指している。

## 重要なルール

### 1. 言語および出力に関する規定

- **原則:** すべての回答、解説、コメント、ドキュメントは**日本語**を使用すること。
- **例外:** エラーメッセージ、ログ出力、システム識別子については英語を許可する。

### 2. コード構成

- 少数の大きなファイルよりも多数の小さなファイル
- 高凝集、低結合
- 通常200-400行、ファイルごとに最大800行
- 型ではなく、機能/ドメインごとに整理

### 3. コードスタイル

- コード、コメント、ドキュメントに絵文字を使用しない
- 常に不変性を保つ - オブジェクトや配列を変更しない
- 本番コードに console.log を使用しない
- try/catchで適切なエラーハンドリング
- Zodなどで入力検証
- **命名規則:** 変数・関数・クラス名はセマンティクス（意味論）を重視し、一目で役割が理解できるものにする。安易な略称（`tmp`, `data1` など）は万死に値する。
- **コメント（強制）:** 
  - コードの「挙動（何をしているか）」ではなく、「意図（なぜそうしたのか）」を記述せよ。
  - 複雑なロジック、ビジネス上の判断根拠、境界条件には、後世のエンジニアが涙を流さないよう注釈を付けること。
  - 自明なコメント（`i++ // iをインクリメント`）は直ちに削除せよ。

### 4. テスト

- TDD: 最初にテストを書く
- 最低80%のカバレッジ
- ユーティリティのユニットテスト
- APIの統合テスト
- 重要なフローのE2Eテスト

### 5. セキュリティ

- ハードコードされた機密情報を使用しない
- 機密データには環境変数を使用
- すべてのユーザー入力を検証
- パラメータ化クエリのみ使用
- CSRF保護を有効化

### 6. エラーハンドリングと堅牢性

- **早期リターン:** ネストを深くするのは思考の迷宮を作るも同義。異常系は冒頭で処理（Early Return）せよ。
- **例外処理:** 例外を握り潰すのは、爆弾の導火線を隠す行為である。適切にキャッチし、適切に報告せよ。
- **型定義:** `any` を使うのは、型安全への敗北宣言である。可能な限り厳密な型を定義せよ。

### 7. 依存関係と再利用性

- **DRY原則:** 「同じコードを2回書いたら、それは共通化の合図」と刻み込め。
- **依存管理:** 外部ライブラリの導入は、セキュリティとメンテナンス状況を確認し、必要最小限に留めよ。

### 8. AI 共同作業の運用規範

- **品質基準:** 構造が明快で、型安全が保証され、保守性が高く、セキュリティ的に堅牢であること。
- **簡潔性:** オーバーエンジニアリングは罪である。最もシンプルで効果的な解を導き出せ。

### 9. 実行環境

Dockerををベースに環境構築しているので、下記コマンドで実行すること。
（下記は`uv run ...`の実行例）

```bash
docker run --rm -it --gpus all -v ${PWD}:/app -w /app -e PREFECT_API_URL=http://host.docker.internal:4200/api sandbox-selenium uv run ...
```
